## Что изменено

- На мобильной ширине шапка стала короче: видны только заголовок, поиск, выбор колоды и свернутый блок `Фильтры`.
- Все второстепенные элементы управления перенесены внутрь спойлера `Фильтры`.
- `rws-classic` переименован в UI как `Классическая колода Таро`.
- `rws-dark-classic` переименован в UI как `Классическая колода Таро, темная`.

## Проверки

- `node --check decks.js`
- `git diff --check`
- статический smoke по наличию `filtersPanel` и runtime-имен колод
- npm checks недоступны: в репозитории нет `package.json`

## Ограничения

- Изменения касаются только UI-шапки и пользовательских label'ов; логика карт, тренировок, прогресса и колод не менялась.
